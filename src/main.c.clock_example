#include "tools/iesusart.h"
#include <stdint.h>
#include <util/delay.h>
#define LOG_LEVEL LOG_LEVEL_TRACE
#include "tools/logger.h"
#include "hardware/clock.h"

int main(void) {
	USART_init(UBRR_SETTING);
	CLOCK_init();
	uint32_t n = ~0;

	while (1) {
		// INFO("%lu %lu\n", CLOCK_get_milliseconds(), CLOCK_get_seconds());
		INFO("%lX\n", n);
	}

	return 0;
}
